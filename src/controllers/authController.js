import mongorito from 'mongorito';
// import userModel from '~/models/userModel.js';

export default {
  basic(request, reply) {
    const {
      username,
      password,
    } = request.payload;   
  },
};
