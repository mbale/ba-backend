general:
  branches:
    only:
      - develop
      - master
deployment:
  staging:
    branch: develop
    commands:
      - sudo scp -i ~/.ssh/id_146.185.169.74 -v -r esi-backend/dist admin@146.185.169.74:/var/backend